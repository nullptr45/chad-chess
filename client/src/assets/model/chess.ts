class Game {
    chessboard: any;
    myColor: boolean;

    constructor(myColor: boolean) {
        this.myColor = myColor;
    }

    // TODO:
    setupBoard() {
        if (this.myColor) {

        } else {

        }
    }

    movePiece(pieceID: number, targetPos: { x: number, y: number }) {
        
    }
}

export default Game;