# BUILD INSTRUCTIONS

## SERVER
```
./gradlew build;
java -jar build/libs/chadchess-01.jar;
```
this gets the server up and running

## CLIENT
```
cd client;
npm i;
npm run build;
```
open `index.html` after build
